<h1>Messages</h1>
<% messages.forEach(msg => { %>
    <div>
      <p><%= msg.user %></p>
      <p><%= msg.text %></p>
      <% const formattedDate = new Date(msg.added).toLocaleString('en-US', {
        weekday: 'short',
        year: 'numeric',
        month: 'numeric',
        day: 'numeric',
        hour: 'numeric',
        minute: '2-digit'
      }) %>
      <p><%= formattedDate %></p>
      <button onclick="location.href='/message?user=<%= encodeURIComponent(msg.user) %>&text=<%= encodeURIComponent(msg.text) %>&date=<%= encodeURIComponent(formattedDate) %>'">
        View Message
      </button>
    </div>
<% }) %>
<br>
<a href='/new'>Post a New Message</a>